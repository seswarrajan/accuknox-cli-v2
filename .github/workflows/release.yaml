name: Release knoxctl

on:
  push:
    tags:
      - '*'

permissions:
  contents: write
  id-token: write

jobs:
  knoxctl-release:
    runs-on: ubuntu-latest
    env:
      GOPRIVATE: github.com/accuknox/*
      GH_ACCESS_TOKEN: ${{ secrets.AK_PAT_REPO_SCOPE }}
      GIT_KEY: ${{ secrets.GIT_KEY }}
    steps:
      -
        name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      -
        name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.20"

      - name: Install Cosign
        uses: sigstore/cosign-installer@main

      - name: Setup access for private go modules
        env:
          GOPRIVATE: github.com/accuknox/*
          USER: accuknox-user
          TOKEN: ${{ secrets.AK_PAT_REPO_SCOPE }}
        run: |
          git config --global url."https://${USER}:${TOKEN}@github.com".insteadOf "https://github.com"
          

      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v5
        with:
          distribution: goreleaser
          version: latest
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Fetch Tag Name
        id: get_tag
        run: echo "::set-output name=TAG_NAME::$(git describe --tags --abbrev=0)"
      
      - name: Checkout knoxctl-website Repository
        uses: actions/checkout@v3
        with:
          repository: accuknox/knoxctl-website
          token: ${{ secrets.AK_PAT_REPO_SCOPE }}
          path: knoxctl-website
          ref: main

      - name: Update latest version file and upload binaries
        run: |
          echo "${{ steps.get_tag.outputs.TAG_NAME }}" > knoxctl-website/static/version/latest_version.txt
          cp -r dist/* knoxctl-website/static/binaries/
          cd knoxctl-website
          git config user.name "accuknox-user"
          git config user.email "devops.alerts@accuknox.com"
          git add .
          git commit -m "[knoxctl-ci-bot] Update latest version and add binaries for version ${{ steps.get_tag.outputs.TAG_NAME }}"


      - name: Push Update to Protected Branch
        uses: CasperWA/push-protected@v2
        with:
          token: ${{ secrets.AK_PAT_REPO_SCOPE }}
          branch: main
        env:
          GIT_COMMITTER_NAME: accuknox-user
          GIT_COMMITTER_EMAIL: devops.alerts@accuknox.com
